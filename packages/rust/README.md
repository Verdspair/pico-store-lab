# PICO Store Lab — Rust SDK

Typed protocol builders, response validators, and mirror policy for the native PICO VRChat package. This crate makes no network requests and can be used independently of the Desktop client.

First public source release: GitHub. crates.io publication is planned for a later release pass.
